# Finance Module Types

type Transaccion {
    id: ID!
    proyecto_id: ID!
    cuenta_id: ID
    categoria_id: ID!
    user_id: ID!
    monto: Float!
    titulo: String
    descripcion: String
    fecha: DateTime!
    numero_factura: String
    fecha_emision: String
    fecha_vencimiento: String
    fecha_pago: DateTime
    notas: String
    status: String!
    is_recurring: Boolean
    recurrence_interval: String
    recurrence_day: Int
    next_occurrence: String
    cuenta: Cuenta @belongsTo
    categoria: Categoria @belongsTo
    created_at: DateTime!
    updated_at: DateTime!
}

type Cuenta {
    id: ID!
    nombre: String!
    banco: String
    tipo: String!
    saldo_inicial: Int!
    saldo_actual: Int!
    estado: String!
    moneda: String
    descripcion: String
    color: String
    icono: String
    tasa_interes_anual: Float
    limite_credito: Int
    transacciones: [Transaccion!] @hasMany
    created_at: DateTime!
    updated_at: DateTime!
}

type Categoria {
    id: ID!
    proyecto_id: ID!
    nombre: String!
    tipo: String!
    transacciones: [Transaccion!] @hasMany
    created_at: DateTime!
    updated_at: DateTime!
}

type BalanceResponse {
    balance: Int!
    pending_bills: Int!
    transaction_count: Int!
}

type PayCreditCardBillResponse {
    transaccion: Transaccion!
    nuevo_saldo_origen: Int!
    nuevo_saldo_tc: Int!
}

# Queries
extend type Query {
    transacciones(proyecto_id: ID! @eq, status: String @eq): [Transaccion!]!
        @guard(with: ["sanctum"])
        @all

    cuentas(proyecto_id: ID!): [Cuenta!]!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\Modules\\Finance\\GraphQL\\Queries\\FinanceQueries@cuentas")

    categorias(proyecto_id: ID! @eq): [Categoria!]!
        @guard(with: ["sanctum"])
        @all
}

# Mutations
extend type Mutation {
    createTransaccion(
        proyecto_id: ID!
        cuenta_id: ID!
        categoria_id: ID!
        monto: Float!
        fecha: String!
        titulo: String
        descripcion: String
        notas: String
        status: String
        cuenta_predeterminada_id: ID
        debito_automatico: Boolean
        is_recurring: Boolean
        recurrence_day: Int
        cuotas: Int
        task_id: ID
        numero_factura: String
        fecha_emision: String
        fecha_vencimiento: String
    ): Transaccion! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@createTransaccion") @guard(with: ["sanctum"])

    updateTransaccion(
        id: ID!
        cuenta_id: ID
        categoria_id: ID
        monto: Float
        fecha: String
        titulo: String
        descripcion: String
        notas: String
        status: String
        numero_factura: String
        fecha_emision: String
        fecha_vencimiento: String
    ): Transaccion! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@updateTransaccion") @guard(with: ["sanctum"])

    deleteTransaccion(id: ID!): Boolean! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@deleteTransaccion") @guard(with: ["sanctum"])

    payBillDirectly(id: ID!): Transaccion! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@payBillDirectly") @guard(with: ["sanctum"])

    createCuenta(
        proyecto_id: ID!
        nombre: String!
        tipo: String!
        saldo_inicial: Int!
        banco: String
        moneda: String
        descripcion: String
        color: String
        icono: String
        tasa_interes_anual: Float
        limite_credito: Int
        monto_desembolsado: Int
        cuenta_destino_id: ID
    ): Cuenta! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@createCuenta") @guard(with: ["sanctum"])

    updateCuenta(
        id: ID!
        proyecto_id: ID!
        nombre: String
        banco: String
        descripcion: String
        saldo_inicial: Int
        color: String
        icono: String
    ): Cuenta! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@updateCuenta") @guard(with: ["sanctum"])

    deleteCuenta(id: ID!, proyecto_id: ID!): Boolean! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@deleteCuenta") @guard(with: ["sanctum"])

    updateCuentaEstado(
        id: ID!
        proyecto_id: ID!
        estado: String!
    ): Cuenta! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@updateCuentaEstado") @guard(with: ["sanctum"])

    payCreditCardBill(
        proyecto_id: ID!
        cuenta_id: ID!
        cuenta_origen_id: ID!
        monto: Int!
        tipo_pago: String!
    ): PayCreditCardBillResponse! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@payCreditCardBill") @guard(with: ["sanctum"])

    createCategoria(
        proyecto_id: ID!
        nombre: String!
        tipo: String!
    ): Categoria! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@createCategoria") @guard(with: ["sanctum"])

    updateCategoria(
        id: ID!
        nombre: String!
        tipo: String!
    ): Categoria! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@updateCategoria") @guard(with: ["sanctum"])

    deleteCategoria(id: ID!): Boolean! @field(resolver: "App\\Modules\\Finance\\GraphQL\\Mutations\\FinanceMutations@deleteCategoria") @guard(with: ["sanctum"])
}
